import React, { FC } from "react";

import Layout from "@components/layout";

const CVPage: FC<{}> = () => {
  return (
    <Layout>
      <main>
        <h2>CV</h2>
      </main>
    </Layout>
  );
};

export default CVPage;
