import React, { FC } from "react";

const NotFoundPage: FC<{}> = () => {
  return (
    <main>
      <title>Not found</title>
      <p>Page not found</p>
    </main>
  );
};

export default NotFoundPage;
